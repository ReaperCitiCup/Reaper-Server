package reaper.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import reaper.model.TotalPortion;

public interface TotalPortionRepository extends JpaRepository<TotalPortion,String>{
}
